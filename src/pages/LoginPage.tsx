export function LoginPage() {
  return (
    <section>
      <h1>Owner login</h1>
      <p className="lede">
        Magic-link / password auth via Supabase will live here. Guests stay
        view-only on the collection.
      </p>
    </section>
  )
}
